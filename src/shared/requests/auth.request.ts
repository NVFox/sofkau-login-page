export type LoginRequest = {
    username: string;
    password: string;
}

export type SignupRequest = {
    email: string;
    firstName: string;
    lastName: string;
    username: string;
    password: string;
}