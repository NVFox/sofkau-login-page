export type LoginRequest = {
    email: string;
    password: string;
}

export type SignupRequest = {
    email: string;
    firstName: string;
    lastName: string;
    username: string;
    password: string;
}